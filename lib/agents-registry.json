{
  "version": "2.0.0",
  "updatedAt": "2026-06-19",
  "agents": [
    {
      "id": "hermes",
      "name": "Hermes — SMB AI Copilot",
      "version": "0.2.0",
      "category": "Orchestration",
      "description": "Multi-channel AI copilot for SMBs. Handles WhatsApp, Email, and Web messaging, classifies intent, generates daily business summaries, and delivers sales & finance insights.",
      "tags": ["orchestration", "smb", "whatsapp", "email", "copilot"],
      "author": "Brahmexa",
      "mcpServer": null,
      "port": 8100,
      "inputSchema": {
        "type": "object",
        "properties": {
          "channel": { "type": "string", "enum": ["whatsapp", "email", "web"] },
          "message": { "type": "string", "description": "Inbound message text" }
        },
        "required": ["channel", "message"]
      },
      "outputSchema": {
        "type": "object",
        "properties": {
          "intent": { "type": "string" },
          "reply": { "type": "string" },
          "summary": { "type": "string" }
        }
      }
    },
    {
      "id": "mercury",
      "name": "Mercury — Startup Advisor",
      "version": "0.2.0",
      "category": "Advisory",
      "description": "AI startup advisor and router — compliance, messaging, support tickets (Nandi), and general coaching for founders and SMB operators.",
      "tags": ["startup", "advisory", "support", "routing"],
      "author": "Brahmexa",
      "mcpServer": null,
      "port": 9000,
      "inputSchema": {
        "type": "object",
        "properties": {
          "message": { "type": "string", "description": "User question or request" }
        },
        "required": ["message"]
      },
      "outputSchema": {
        "type": "object",
        "properties": {
          "response": { "type": "string" },
          "source": { "type": "string" },
          "intent": { "type": "string" }
        }
      }
    }
  ]
}
